kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: clonarr-config
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: Immediate
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: clonarr-config
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: clonarr-config
  local:
    path: /data/local/clonarr
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clonarr-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: clonarr-config
  resources:
    requests:
      storage: 100Gi
---