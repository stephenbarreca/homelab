kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: pulsarr-config
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: Immediate
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pulsarr-config
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: pulsarr-config
  local:
    path: /data/local/pulsarr
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulsarr-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: pulsarr-config
  resources:
    requests:
      storage: 5Gi
---