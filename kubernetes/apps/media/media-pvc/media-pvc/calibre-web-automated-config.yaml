kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: calibre-web-automated-config
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: Immediate
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: calibre-web-automated-config
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: calibre-web-automated-config
  local:
    path: /data/local/calibre-web-automated
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-automated-config
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: calibre-web-automated-config
  resources:
    requests:
      storage: 100Gi
---